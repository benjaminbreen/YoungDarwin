# Architecture

The maintained architecture guide lives at `docs/architecture.md`.

This root file is kept as a compatibility pointer for tools or links that expect
`architecture.md` at the repository root.
