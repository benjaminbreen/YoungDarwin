# Shared Period Lighting Textures

- `aged-brass-roughness.png`: project-authored deterministic noise map generated
  with ImageMagick (seed `1835`). This tracked runtime copy is also the source
  consumed by the rebuild script.
- Embedded in the shared period lamp and candlestick GLBs by
  `scripts/blender_build_period_lighting_props.py`.
